# Git LFS Policy

Use Git LFS for generated or imported mesh and texture binaries under `assets/`: `.glb`, `.gltf`, `.bin`, `.fbx`, `.png`, `.jpg`, and `.jpeg`.

Do not commit generated binary assets during scaffold work. Add only source metadata, documentation, and small text fixtures unless a later issue explicitly authorizes asset import.

